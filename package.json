{
  "name": "lamylenoise-web",
  "version": "1.2.0",
  "private": true,
  "description": "LAMYLENOISE marketplace frontend and trusted Firebase backend.",
  "scripts": {
    "build": "node scripts/validate-static-site.mjs",
    "test:functions": "npm --prefix functions install --ignore-scripts && npm --prefix functions test",
    "test:rules": "firebase emulators:exec --project lamylenoise-rules-test --only database \"npm --prefix functions run test:rules\"",
    "check": "npm run build && npm run test:functions",
    "emulators": "firebase emulators:start --only auth,database,functions,hosting",
    "deploy": "node scripts/deploy-safe.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "firebase-tools": "latest"
  }
}
