'use strict';
const{getApps,initializeApp}=require('firebase-admin/app');const{getAuth}=require('firebase-admin/auth');const{getDatabase}=require('firebase-admin/database');const{HttpsError,onCall}=require('firebase-functions/v2/https');const audit=require('./audit');const{PERMISSIONS,adminSummary,normalizePermissions,permissionMap}=require('./admin-governance-core');if(!getApps().length)initializeApp();const db=getDatabase(),REGION='me-central1',DEFAULT_TENANT='lamylenoise';function clean(v,m=160){return String(v??'').trim().slice(0,m)}
async function owner(request){if(!request.auth?.uid)throw new HttpsError('unauthenticated','Connectez-vous.');const profile=(await db.ref(`profiles/${request.auth.uid}`).get()).val(),token=request.auth.token||{};if(!profile||profile.status==='disabled'||profile.role!=='admin'||profile.isSuperAdmin!==true||token.role!=='admin'||token.isSuperAdmin!==true)throw new HttpsError('permission-denied','Seul le propriétaire peut gérer les administrateurs délégués.');return{uid:request.auth.uid,tenantId:clean(profile.tenantId||DEFAULT_TENANT,80)||DEFAULT_TENANT}}
async function viewer(request){if(!request.auth?.uid)throw new HttpsError('unauthenticated','Connectez-vous.');const profile=(await db.ref(`profiles/${request.auth.uid}`).get()).val(),token=request.auth.token||{},isOwner=profile?.isSuperAdmin===true&&token.isSuperAdmin===true;const canRead=profile?.adminPermissions?.['delegatedAdmin.read']===true||profile?.adminPermissions?.['*']===true;if(!profile||profile.status==='disabled'||profile.role!=='admin'||token.role!=='admin'||(!isOwner&&!canRead))throw new HttpsError('permission-denied','Permission gouvernance insuffisante.');return{uid:request.auth.uid,tenantId:clean(profile.tenantId||DEFAULT_TENANT,80)||DEFAULT_TENANT,isOwner}}
function tenantFor(value,own){const tenant=clean(value||own,80)||DEFAULT_TENANT;if(tenant!==own)throw new HttpsError('permission-denied','Organisation non autorisée.');return tenant}
exports.getDelegatedAdmins=onCall({region:REGION,maxInstances:5},async request=>{const admin=await viewer(request),tenantId=tenantFor(request.data?.tenantId,admin.tenantId),snap=await db.ref('profiles').orderByChild('tenantId').equalTo(tenantId).limitToLast(250).get();const admins=Object.entries(snap.val()||{}).filter(([,p])=>p.role==='admin').map(([uid,p])=>adminSummary({uid,...p})).sort((a,b)=>Number(b.isSuperAdmin)-Number(a.isSuperAdmin)||a.name.localeCompare(b.name));return{tenantId,viewer:{isOwner:admin.isOwner},permissionCatalog:PERMISSIONS,admins,truncated:snap.numChildren()>=250}});
exports.updateDelegatedAdmin=onCall({region:REGION,maxInstances:3},async request=>{const admin=await owner(request),tenantId=tenantFor(request.data?.tenantId,admin.tenantId),targetUid=clean(request.data?.targetUid),action=clean(request.data?.action,40),reason=clean(request.data?.reason,500),permissions=normalizePermissions(request.data?.permissions);if(!targetUid||targetUid===admin.uid||!['promote','set_permissions','disable','enable','demote'].includes(action))throw new HttpsError('invalid-argument','Action de délégation invalide.');if(reason.length<5)throw new HttpsError('invalid-argument','Motif obligatoire.');if(['promote','set_permissions'].includes(action)&&permissions.length===0)throw new HttpsError('invalid-argument','Sélectionnez au moins une permission.');let problem=null,nextRole='';const now=Date.now(),ref=db.ref(`profiles/${targetUid}`),tx=await ref.transaction(profile=>{if(!profile){problem=new HttpsError('not-found','Profil introuvable.');return}if(clean(profile.tenantId||DEFAULT_TENANT,80)!==tenantId){problem=new HttpsError('permission-denied','Profil d’une autre organisation.');return}if(profile.isSuperAdmin===true){problem=new HttpsError('failed-precondition','Un propriétaire ne peut pas être modifié.');return}if(action==='promote'&&profile.role==='admin'){problem=new HttpsError('already-exists','Ce profil est déjà administrateur.');return}if(action!=='promote'&&profile.role!=='admin'){problem=new HttpsError('failed-precondition','Le profil n’est pas administrateur délégué.');return}if(action==='promote'){profile.delegatedPreviousRole=['customer','seller','courier'].includes(profile.role)?profile.role:'customer';profile.role='admin';profile.status='active';profile.adminPermissions=permissionMap(permissions)}if(action==='set_permissions')profile.adminPermissions=permissionMap(permissions);if(action==='disable')profile.status='disabled';if(action==='enable')profile.status='active';if(action==='demote'){profile.role=['customer','seller','courier'].includes(profile.delegatedPreviousRole)?profile.delegatedPreviousRole:'customer';profile.adminPermissions=null;profile.delegatedPreviousRole=null;profile.status='active'}profile.claimsSyncStatus='pending';profile.claimsSyncError=null;profile.updatedAt=now;profile.updatedBy=admin.uid;profile.lastGovernanceReason=reason;nextRole=profile.role;return profile},undefined,false);if(!tx.committed)throw problem||new HttpsError('aborted','Mise à jour impossible.');try{const user=await getAuth().getUser(targetUid),claims={...(user.customClaims||{}),role:nextRole,tenantId};delete claims.isSuperAdmin;await getAuth().setCustomUserClaims(targetUid,claims);await ref.update({claimsSyncStatus:'complete',claimsSyncedAt:Date.now(),claimsSyncError:null})}catch(error){await ref.update({claimsSyncStatus:'failed',claimsSyncError:clean(error?.message||'Claims sync failed',300)});throw new HttpsError('internal','Profil sécurisé, mais la synchronisation Auth doit être relancée.')}await audit.appendAuditEvent({tenantId,action:`delegated_admin.${action}`,entityType:'admin_profile',entityId:targetUid,actorUid:admin.uid,actorType:'super_admin',source:'callable',changedKeys:['role','status','adminPermissions'],after:{role:nextRole,status:tx.snapshot.val().status,permissionCount:permissions.length},metadata:{reasonProvided:true}});return{targetUid,role:nextRole,status:tx.snapshot.val().status,claimsSyncStatus:'complete'}});
