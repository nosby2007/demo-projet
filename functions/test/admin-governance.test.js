'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{PERMISSIONS,adminSummary,maskEmail,normalizePermissions,permissionMap}=require('../admin-governance-core');test('permission catalog includes every enterprise module',()=>{for(const p of['dashboard.read','support.write','risk.write','system.write','campaign.write','delegatedAdmin.read'])assert.ok(PERMISSIONS.includes(p))});test('unknown and duplicate permissions are rejected deterministically',()=>assert.deepEqual(normalizePermissions(['risk.read','bad','risk.read']),['risk.read']));test('permission maps never grant wildcard or write unknown keys',()=>assert.deepEqual(permissionMap(['*','support.read']),{'support.read':true}));test('admin summaries mask identity and private governance reasons',()=>{const row=adminSummary({uid:'u',email:'person@example.com',role:'admin',adminPermissions:{'risk.read':true},lastGovernanceReason:'private'});assert.equal(row.emailMasked,'pe***@example.com');assert.equal('lastGovernanceReason'in row,false)});test('email masking tolerates absent values',()=>assert.equal(maskEmail(''),''));
