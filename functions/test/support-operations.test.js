'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{caseSummary,slaFor,summarizeCases}=require('../support-core');test('priority SLA targets are deterministic',()=>{assert.equal(slaFor(0,'critical'),3600000);assert.equal(slaFor(0,'normal'),43200000)});test('only active overdue cases breach SLA',()=>{assert.equal(caseSummary({createdAt:1,slaDueAt:10,status:'open'},20).slaBreached,true);assert.equal(caseSummary({createdAt:1,slaDueAt:10,status:'resolved'},20).slaBreached,false)});test('queue summary counts operational risk',()=>{const q=summarizeCases([{id:'a',createdAt:1,slaDueAt:2,status:'open',priority:'critical'},{id:'b',createdAt:1,status:'resolved'}],10);assert.deepEqual([q.summary.open,q.summary.unassigned,q.summary.breached,q.summary.critical],[1,1,1,1])});test('summaries exclude descriptions and notes',()=>{const c=caseSummary({id:'a',createdAt:1,description:'secret',lastAdminNote:'private'});assert.equal('description'in c,false);assert.equal('lastAdminNote'in c,false)});
