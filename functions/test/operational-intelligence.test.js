'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{compactContext,deterministicBrief,normalizeBrief}=require('../operational-intelligence-core');test('context contains aggregates without raw records',()=>{const c=compactContext({now:10,support:{x:{status:'open',slaDueAt:1,email:'secret'}}});assert.equal(c.support.breached,1);assert.equal(JSON.stringify(c).includes('secret'),false)});test('fallback flags human approval',()=>assert.ok(deterministicBrief(compactContext({})).actions.every(x=>x.requiresHumanApproval)));test('critical signals produce priority actions',()=>{const b=deterministicBrief({support:{breached:0},risk:{critical:2},system:{status:'healthy'}});assert.equal(b.actions[0].priority,'critical')});test('model output cannot disable approval',()=>{const f=deterministicBrief(compactContext({})),b=normalizeBrief({actions:[{requiresHumanApproval:false,title:'x'}]},f);assert.equal(b.actions[0].requiresHumanApproval,true)});
