/* Stripe publishable key — safe to expose client-side (never the secret key). */
'use strict';

// TODO: replace with the real Stripe publishable key before enabling card payments in production.
window.STRIPE_PUBLISHABLE_KEY = 'pk_test_REPLACE_WITH_YOUR_STRIPE_PUBLISHABLE_KEY';
