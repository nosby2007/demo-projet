/* Firebase Cloud Messaging Web Push certificate (VAPID) key — safe to expose client-side. */
'use strict';

// TODO: replace with the real Web Push certificate key from Firebase Console
// (Project settings -> Cloud Messaging -> Web configuration) before push notifications will work.
window.FCM_VAPID_KEY = 'REPLACE_WITH_YOUR_FCM_VAPID_KEY';
