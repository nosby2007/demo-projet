import{readFile}from'node:fs/promises';import vm from'node:vm';const e=[];const[core,back,main,html,run,sw,doc]=await Promise.all(['functions/operational-intelligence-core.js','functions/operational-intelligence.js','functions/main.js','admin.html','admin-insights-runtime.js','service-worker.js','docs/admin-control-center.md'].map(p=>readFile(p,'utf8')));for(const[n,s]of[['core',core],['backend',back],['runtime',run]])try{new vm.Script(s,{filename:n})}catch(x){e.push(`${n}: ${x.message}`)}for(const x of['defineSecret','OPENAI_API_KEY','responses.create','json_schema','store:false','insights.generate'])if(!back.includes(x))e.push(`backend missing ${x}`);if(!main.includes("require('./operational-intelligence')"))e.push('functions not composed');if(!html.includes('admin-insights-runtime.js')||!sw.includes("'/admin-insights-runtime.js'"))e.push('runtime not wired');if(run.includes('.ref(')||run.includes('firebase.database'))e.push('UI reads RTDB');for(const x of['Phase 9','insights.generate'])if(!doc.includes(x))e.push(`docs missing ${x}`);if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('Operational Intelligence validation passed. Secret isolation, structured output, aggregate-only context and human approval are enforced.');
