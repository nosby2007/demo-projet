import{readFile}from'node:fs/promises';import vm from'node:vm';const e=[],read=p=>readFile(p,'utf8');const[core,backend,main,rules,html,runtime,sw,doc]=await Promise.all(['functions/support-core.js','functions/support-operations.js','functions/main.js','database.rules.json','admin.html','admin-support-runtime.js','service-worker.js','docs/admin-control-center.md'].map(read));for(const[n,s]of[['core',core],['backend',backend],['runtime',runtime]])try{new vm.Script(s,{filename:n})}catch(x){e.push(`${n}: ${x.message}`)}for(const x of['createSupportCase','getAdminSupportQueue','updateAdminSupportCase','support.read','support.write','slaFor','transaction','auditSupportCaseWrites'])if(!backend.includes(x))e.push(`backend missing ${x}`);for(const x of['SLA_MS','slaBreached','summarizeCases'])if(!core.includes(x))e.push(`core missing ${x}`);if(!main.includes("require('./support-operations')")||!main.includes('...supportOperations'))e.push('support functions not composed');const r=JSON.parse(rules).rules;if(r.supportCases?.['.read']!==false||r.supportCases?.['.write']!==false)e.push('support cases must deny browser access');for(const x of['admin-support-runtime.js','firebase-functions-compat.js'])if(!html.includes(x))e.push(`admin missing ${x}`);for(const x of["call('getAdminSupportQueue')","call('updateAdminSupportCase')",'data-enterprise-tab'])if(!runtime.includes(x))e.push(`runtime missing ${x}`);if(runtime.includes('.ref(')||runtime.includes('firebase.database'))e.push('support UI must not read RTDB');if(!sw.includes("'/admin-support-runtime.js'"))e.push('support runtime not cached');for(const x of['Phase 4','support.read','support.write'])if(!doc.includes(x))e.push(`docs missing ${x}`);if(e.length){console.error(e.join('\n'));process.exit(1)}console.log('Support Operations validation passed. SLA, privacy, RBAC, audit and trusted mutations are enforced.');
